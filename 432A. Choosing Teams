#include <bits/stdc++.h>
using namespace std;

int main() {
    int n,k;
    cin>>n>>k;
    int count=0;
    int x;
    for(int i=0;i<n;i++)
    {
       cin>>x;
       if(5-x>=k)
       {
           count++;
       }
    }
    cout<<count/3<<endl;
    
	return 0;
}
